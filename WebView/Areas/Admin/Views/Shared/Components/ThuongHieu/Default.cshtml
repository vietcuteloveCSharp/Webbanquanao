@model IEnumerable<DAL.Entities.ThuongHieu>

@foreach (var item in Model)
{
    <div class="panel panel-default">
        <div class="panel-heading">
            <h4 class="panel-title">
                <a asp-action="Index" asp-controller="ThuongHieu" asp-route-id="@item.Id">
                    @item.Ten
                </a>
            </h4>
        </div>
    </div>
}
